import { t as GATEWAY_RESTART_REPLACEMENT_TIMEOUT_MS } from "./gateway-shutdown-budget-E5oPIr_h.mjs";
//#region src/cli/daemon-cli/restart-health.constants.ts
const DEFAULT_RESTART_HEALTH_TIMEOUT_MS = GATEWAY_RESTART_REPLACEMENT_TIMEOUT_MS;
const DEFAULT_RESTART_HEALTH_DELAY_MS = 500;
const DEFAULT_RESTART_HEALTH_ATTEMPTS = Math.ceil(DEFAULT_RESTART_HEALTH_TIMEOUT_MS / 500);
const INTERRUPTED_UPDATE_SETTLE_PROBES = Math.ceil(DEFAULT_RESTART_HEALTH_ATTEMPTS / 10);
const INTERRUPTED_UPDATE_SETTLE_TIMEOUT_MS = Math.floor(DEFAULT_RESTART_HEALTH_TIMEOUT_MS / 4) + (INTERRUPTED_UPDATE_SETTLE_PROBES - 1) * 500 + 3e4;
//#endregion
export { INTERRUPTED_UPDATE_SETTLE_TIMEOUT_MS as a, INTERRUPTED_UPDATE_SETTLE_PROBES as i, DEFAULT_RESTART_HEALTH_DELAY_MS as n, DEFAULT_RESTART_HEALTH_TIMEOUT_MS as r, DEFAULT_RESTART_HEALTH_ATTEMPTS as t };
