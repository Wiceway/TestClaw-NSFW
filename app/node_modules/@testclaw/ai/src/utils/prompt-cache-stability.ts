/**
 * STUB (testclaw): логика стабильности кэша промта вырезана.
 * Оригинал: packages/ai/src/utils/prompt-cache-stability.ts
 */
export function sortPromptCacheToolsByName<T>(tools: T[]): T[] {
  return tools;
}

export function normalizeStructuredPromptSection(text: string): string {
  return text;
}

export function normalizePromptCapabilityIds(capabilities: ReadonlyArray<string>): string[] {
  return [...capabilities];
}
