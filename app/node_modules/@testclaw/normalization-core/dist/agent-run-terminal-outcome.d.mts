//#region packages/normalization-core/src/agent-run-terminal-outcome.d.ts
export declare const AGENT_RUN_ABORTED_STOP_REASON: "aborted";
export declare const AGENT_RUN_RESTART_ABORT_STOP_REASON: "restart";
export declare const AGENT_RUN_SUPERSEDED_STOP_REASON: "superseded";
declare const AGENT_RUN_TIMEOUT_PHASES: readonly ["queue", "preflight", "provider", "post_turn", "gateway_draining"];
export type AgentRunTimeoutPhase = (typeof AGENT_RUN_TIMEOUT_PHASES)[number];
type AgentRunWaitStatus = "ok" | "error" | "timeout";
type AgentRunTerminalReason = "completed" | "hard_timeout" | "timed_out" | "superseded" | "cancelled" | "aborted" | "blocked" | "abandoned" | "failed";
export type AgentRunTerminalFacts = {
  reason: AgentRunTerminalReason;
  status: AgentRunWaitStatus;
  stopReason?: string;
  livenessState?: string;
  timeoutPhase?: AgentRunTimeoutPhase;
  providerStarted?: boolean;
};
export type AgentRunTerminalFactInput = {
  status: AgentRunWaitStatus;
  stopReason?: unknown;
  livenessState?: unknown;
  timeoutPhase?: unknown;
  providerStarted?: unknown;
};
type AgentRunWaitTerminalFactInput = Omit<AgentRunTerminalFactInput, "status"> & {
  status?: unknown;
};
type AgentRunLifecycleTerminalFactInput = AgentRunWaitTerminalFactInput & {
  aborted?: unknown;
};
export declare function normalizeAgentRunTimeoutPhase(value: unknown): AgentRunTimeoutPhase | undefined;
export declare function normalizeProviderStarted(value: unknown): boolean | undefined;
export declare function isAbortedAgentStopReason(value: unknown): value is typeof AGENT_RUN_ABORTED_STOP_REASON | typeof AGENT_RUN_RESTART_ABORT_STOP_REASON;
export declare function classifyAgentRunTerminalOutcome(outcome: Pick<AgentRunTerminalFacts, "reason">): "success" | "timeout" | "cancellation" | "failure";
/** Interprets terminal metadata without runtime abort, error formatting, or storage ownership. */
export declare function resolveAgentRunTerminalFacts(input: AgentRunTerminalFactInput): AgentRunTerminalFacts;
/** Applies lifecycle flags before interpreting the same canonical terminal metadata. */
export declare function resolveAgentRunLifecycleTerminalFacts(input: {
  phase: "end" | "error";
  data?: AgentRunLifecycleTerminalFactInput;
  abortFields?: {
    aborted?: true;
    stopReason?: string;
  };
}): AgentRunTerminalFacts;
/** Reads reduced terminal wait snapshots; pending/deadline admission belongs to the caller. */
export declare function resolveAgentRunWaitTerminalFacts(input: AgentRunWaitTerminalFactInput): AgentRunTerminalFacts | undefined;
/** Reads the execution owner's publication fact; it grants no runtime authority. */
export declare function hasExecutionSettlement(data?: Record<string, unknown>): boolean;
/** Distinguishes terminal outcomes from errors that can still be followed by a retry. */
export declare function isDefinitiveRunLifecycle(input: {
  phase?: unknown;
  data?: Record<string, unknown>;
}): boolean;
//#endregion