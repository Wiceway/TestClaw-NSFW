//#region packages/normalization-core/src/grapheme.d.ts
/**
 * Chooses a whole-grapheme cut within the hard budget, honoring a usable preference.
 * If no whole grapheme fits, allowPartial permits a surrogate-safe progress cut;
 * a leading surrogate pair can exceed maxEnd by one code unit.
 */
export declare function findGraphemeChunkEnd(text: string, start: number, maxEnd: number, preferredEnd?: number, allowPartial?: boolean): number;
/** Width to reserve for the first whole grapheme, or zero for empty text. */
export declare function firstGraphemeClusterLength(text: string): number;
/** Skips only whole whitespace graphemes, never the base of a space-plus-mark cluster. */
export declare function skipWhitespaceGraphemes(text: string, start?: number, maxGraphemes?: number): number;
/** Trims only whole trailing whitespace graphemes from a source prefix. */
export declare function trimEndWhitespaceGraphemes(text: string, end?: number): string;
//#endregion