//#region packages/normalization-core/src/utf16-slice.d.ts
/** Moves a chunk boundary away from the middle of a UTF-16 surrogate pair. */
export declare function avoidTrailingHighSurrogateBreak(text: string, start: number, end: number): number;
/** Slices a UTF-16 string without returning dangling surrogate halves at either edge. */
export declare function sliceUtf16Safe(input: string, start: number, end?: number): string;
/** Truncates a UTF-16 string without cutting a surrogate pair in half. */
export declare function truncateUtf16Safe(input: string, maxLen: number): string;
/** Truncates text and appends a marker while preserving the caller's reserved width contract. */
export declare function truncateWithMarker(value: string, max: number, options: {
  marker: string;
  reserve: number;
  trimEnd: boolean;
}): string;
//#endregion