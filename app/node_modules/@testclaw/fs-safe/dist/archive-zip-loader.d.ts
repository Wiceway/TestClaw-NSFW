import type { ZipDirectoryEntry } from "./archive-zip-directory.js";
import { type ZipEntry } from "./archive-zip-entry.js";
export type ZipArchiveWithFiles = {
    files: Record<string, unknown>;
};
export declare function admittedZipEntries(archive: ZipArchiveWithFiles): readonly ZipEntry[];
/** Internal: the caller has admitted these unchanged bytes and their metadata. */
export declare function loadAdmittedZipArchive(buffer: Buffer | Uint8Array, admitted: ZipDirectoryEntry[]): Promise<ZipArchiveWithFiles>;
