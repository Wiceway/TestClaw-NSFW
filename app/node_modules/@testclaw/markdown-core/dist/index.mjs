import { findFenceSpanAt, isSafeFenceBreak, parseFenceSpans, scanFenceSpans } from "./fences.mjs";
import { buildCodeSpanIndex, createInlineCodeState } from "./code-spans.mjs";
import { i as stripFrontmatterBlock, n as parseFrontmatterBlock, r as parseFrontmatterBlockResult, t as extractFrontmatterBlock } from "./frontmatter-CiMm0JLF.mjs";
import { a as markdownToIR, b as chunkText, d as tokenizeHtmlTags, f as sliceMarkdownIR, g as isAutoLinkedMarkdownLink, i as getMarkdownTableSource, n as chunkMarkdownIR, o as markdownToIRWithMeta, r as countMarkdownFencedCodeChars, t as appendMarkdownIR, u as matchMarkdownHtmlTag, w as avoidTrailingHighSurrogateBreak, x as chunkTextRanges } from "./ir-DnPCa0o8.mjs";
import { n as applyConstructFallbacks, t as renderMarkdownWithMarkers } from "./render-D04rDh_w.mjs";
import { renderMarkdownIRChunksWithinLimit } from "./render-aware-chunking.mjs";
import { t as convertMarkdownTables } from "./tables-l3Utyb6j.mjs";
import "./types.mjs";
//#region packages/markdown-core/src/format-capabilities.ts
const NATIVE_FORMAT_CONSTRUCTS = {
	bold: "native",
	italic: "native",
	underline: "native",
	strikethrough: "native",
	spoiler: "native",
	codeInline: "native",
	codeBlock: "native",
	codeLanguage: "native",
	linkLabel: "native",
	heading: "native",
	bulletList: "native",
	orderedList: "native",
	taskList: "native",
	table: "native",
	blockquote: "native",
	image: "native",
	mention: "native"
};
/** Defines a channel profile with native support as the default for each construct. */
function defineFormatProfile(profile) {
	return {
		...profile,
		constructs: {
			...NATIVE_FORMAT_CONSTRUCTS,
			...profile.constructs
		}
	};
}
/** Runtime helpers for defining static channel formatting capabilities. */
const FormatCapabilityProfile = { define: defineFormatProfile };
//#endregion
//#region packages/markdown-core/src/render-attributed.ts
/** Renders Markdown IR into text plus UTF-16 style ranges for attributed-text targets. */
function renderMarkdownWithAttributedRanges(ir, options, profile) {
	const projected = profile ? applyConstructFallbacks(ir, profile) : ir;
	const text = projected.text ?? "";
	const insertions = [];
	let rendered = text;
	if (options.renderLink) {
		rendered = "";
		let cursor = 0;
		for (const link of projected.links.toSorted((a, b) => a.start - b.start)) {
			if (link.start < cursor) continue;
			rendered += text.slice(cursor, link.end);
			const origin = isAutoLinkedMarkdownLink(link) ? "linkify" : "authored";
			const suffix = options.renderLink(link, text, { origin });
			rendered += suffix;
			if (suffix) insertions.push({
				pos: link.end,
				length: suffix.length
			});
			cursor = link.end;
		}
		rendered += text.slice(cursor);
	}
	rendered = options.trimEnd ? rendered.trimEnd() : rendered;
	const ranges = [];
	const appendRange = (offset, end, style) => {
		const start = Math.max(0, Math.min(offset, rendered.length));
		const length = Math.min(end, rendered.length) - start;
		if (length > 0) ranges.push({
			start,
			length,
			style
		});
	};
	const appendSpan = (span, style) => {
		if (style === void 0) return;
		let cursor = span.start;
		let shift = 0;
		for (const insertion of insertions) if (insertion.pos <= cursor) shift += insertion.length;
		else if (insertion.pos >= span.end) break;
		else {
			appendRange(cursor + shift, insertion.pos + shift, style);
			cursor = insertion.pos;
			shift += insertion.length;
		}
		appendRange(cursor + shift, span.end + shift, style);
	};
	projected.styles.forEach((span) => appendSpan(span, options.styleMap[span.style]));
	for (const annotation of projected.annotations ?? []) appendSpan(annotation, options.annotationStyleMap?.[annotation.type]);
	ranges.sort((a, b) => a.start - b.start || a.length - b.length || a.style.localeCompare(b.style));
	let retained = 0;
	for (const range of ranges) {
		const previous = ranges[retained - 1];
		if (previous && previous.style === range.style && range.start <= previous.start + previous.length) previous.length = Math.max(previous.start + previous.length, range.start + range.length) - previous.start;
		else {
			ranges[retained] = range;
			retained += 1;
		}
	}
	ranges.length = retained;
	return {
		text: rendered,
		ranges
	};
}
//#endregion
export { FormatCapabilityProfile, appendMarkdownIR, applyConstructFallbacks, avoidTrailingHighSurrogateBreak, buildCodeSpanIndex, chunkMarkdownIR, chunkText, chunkTextRanges, convertMarkdownTables, countMarkdownFencedCodeChars, createInlineCodeState, extractFrontmatterBlock, findFenceSpanAt, getMarkdownTableSource, isSafeFenceBreak, markdownToIR, markdownToIRWithMeta, matchMarkdownHtmlTag, parseFenceSpans, parseFrontmatterBlock, parseFrontmatterBlockResult, renderMarkdownIRChunksWithinLimit, renderMarkdownWithAttributedRanges, renderMarkdownWithMarkers, scanFenceSpans, sliceMarkdownIR, stripFrontmatterBlock, tokenizeHtmlTags };
