import { t as MarkdownTableMode } from "./types-C6mGxBC3.mjs";
//#region packages/markdown-core/src/html-tags.d.ts
declare function matchMarkdownHtmlTag(source: string): string | undefined;
type HtmlTagToken = {
  raw: string;
  start: number;
  end: number;
  name: string;
  closing: boolean;
  selfClosing: boolean;
};
/** Tokenizes valid open/close HTML tags with Markdown-It's quote-aware grammar. */
declare function tokenizeHtmlTags(html: string): Generator<HtmlTagToken>;
//#endregion
//#region packages/markdown-core/src/ir-metadata.d.ts
type MarkdownHtmlMetadata = {
  /** Complete authored HTML tags in UTF-16 offsets; omitted from serialized IR. */
  htmlTags?: HtmlTagToken[];
};
type MarkdownListItemMarker = {
  kind: "bullet" | "ordered";
  listMarker?: {
    start: number;
    end: number;
  };
  task?: true;
  taskMarker?: {
    start: number;
    end: number;
  };
  /** Parser-owned identity and rendered span for block-native list emitters. */
  listId?: number;
  parentListId?: number;
  depth?: number;
  start?: number;
  end?: number;
};
//#endregion
//#region packages/markdown-core/src/assistant-transcript-headers.d.ts
type AssistantTranscriptRole = "assistant" | "developer" | "system" | "user";
type AssistantTranscriptRoleHeaderKind = "angle_role_header" | "role_timestamp_bracket" | "timestamp_role_colon";
//#endregion
//#region packages/markdown-core/src/ir-spans.d.ts
type MarkdownStyle = "bold" | "italic" | "underline" | "strikethrough" | "code" | "code_block" | "spoiler" | "blockquote" | "heading_1" | "heading_2" | "heading_3" | "heading_4" | "heading_5" | "heading_6";
type MarkdownStyleSpan = {
  start: number;
  end: number;
  style: MarkdownStyle;
  language?: string;
};
type MarkdownLinkSpan = {
  start: number;
  end: number;
  href: string;
};
type MarkdownAnnotationSpan = {
  start: number;
  end: number;
  type: "assistant_transcript_role";
  kind: AssistantTranscriptRoleHeaderKind;
  role: AssistantTranscriptRole;
};
//#endregion
//#region packages/markdown-core/src/ir-slice.d.ts
declare function sliceMarkdownIR(ir: MarkdownIR, start: number, end: number): MarkdownIR;
//#endregion
//#region packages/markdown-core/src/ir.d.ts
type MarkdownIR = MarkdownHtmlMetadata & {
  text: string;
  styles: MarkdownStyleSpan[];
  links: MarkdownLinkSpan[];
  annotations?: MarkdownAnnotationSpan[];
  listItems?: MarkdownListItemMarker[];
};
type MarkdownTableAlignment = "left" | "center" | "right";
type MarkdownTableData = {
  headers: string[];
  rows: string[][];
  aligns?: (MarkdownTableAlignment | undefined)[];
};
type MarkdownTableCell = MarkdownHtmlMetadata & {
  text: string;
  styles: MarkdownStyleSpan[];
  links: MarkdownLinkSpan[];
  annotations?: MarkdownAnnotationSpan[];
};
type MarkdownTableMeta = MarkdownTableData & {
  placeholderOffset: number;
  headerCells: MarkdownTableCell[];
  rowCells: MarkdownTableCell[][];
};
type MarkdownTableSource = {
  start: number;
  end: number;
  prefix: string;
  headers: string[];
  rows: string[][];
};
type MarkdownTableWithSource = MarkdownTableMeta & {
  source?: MarkdownTableSource;
};
/** Private source coordinates do not change the serialized native-table payload. */
declare function getMarkdownTableSource(table: MarkdownTableWithSource): MarkdownTableSource | undefined;
type MarkdownParseOptions = {
  /** Mark assistant-authored transcript-role headers after Markdown parsing. */
  assistantTranscriptRoleHeaders?: boolean;
  linkify?: boolean;
  enableSpoilers?: boolean;
  /** Parse authored HTML <u>/<ins> tags into underline spans. */
  enableHtmlUnderline?: boolean;
  /** Preserve task-list checkboxes as semantic list markers. */
  enableTaskLists?: boolean;
  headingStyle?: "none" | "bold" | "rich";
  blockquotePrefix?: string;
  autolink?: boolean;
  /** How to render tables (off|bullets|code|block). Default: off. */
  tableMode?: MarkdownTableMode;
  /** Visible text emitted for a thematic break. Default: ───. */
  horizontalRuleText?: string;
  /** Preserve source line spacing after headings and code blocks. */
  preserveSourceBlockSpacing?: boolean;
  /**
   * Treat Python-style `__name__` member, call, argument, and index identifiers as literal text
   * instead of emphasis delimiters. Disabled by default.
   */
  preserveDunderIdentifiers?: boolean;
};
/** Count fenced code body characters using the same block grammar as rendering. */
declare function countMarkdownFencedCodeChars(markdown: string): number;
/** Transfers a separately owned IR slice, including its metadata, into an accumulator. */
declare function appendMarkdownIR(target: MarkdownIR, source: MarkdownIR): void;
declare function markdownToIR(markdown: string, options?: MarkdownParseOptions): MarkdownIR;
declare function markdownToIRWithMeta(markdown: string, options?: MarkdownParseOptions): {
  ir: MarkdownIR;
  hasTables: boolean;
  tables: MarkdownTableMeta[];
};
declare function chunkMarkdownIR(ir: MarkdownIR, limit: number): MarkdownIR[];
//#endregion
export { HtmlTagToken as _, MarkdownTableMeta as a, countMarkdownFencedCodeChars as c, markdownToIRWithMeta as d, sliceMarkdownIR as f, MarkdownStyleSpan as g, MarkdownStyle as h, MarkdownTableData as i, getMarkdownTableSource as l, MarkdownLinkSpan as m, MarkdownParseOptions as n, appendMarkdownIR as o, MarkdownAnnotationSpan as p, MarkdownTableCell as r, chunkMarkdownIR as s, MarkdownIR as t, markdownToIR as u, matchMarkdownHtmlTag as v, tokenizeHtmlTags as y };