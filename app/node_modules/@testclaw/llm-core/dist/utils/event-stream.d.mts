import { $ as ThinkingContent, a as AssistantMessage, o as AssistantMessageEvent, s as AssistantMessageEventStreamContract } from "../types-BrJ7E2UK.mjs";
//#region packages/llm-core/src/utils/event-stream.d.ts
/** Package-internal mutation fact for native reasoning projection. */
export declare function appendAssistantThinking(block: ThinkingContent, delta: string): void;
/** Returns only an append whose current and previously observed snapshots still match. */
export declare function readAssistantThinkingAppend(block: ThinkingContent, previous: string): string | undefined;
/** Observe native producer settlement without invoking consumer result decorators. */
export declare function getEventStreamCompletion(stream: object): Promise<unknown> | undefined;
/** Generic async-iterable event stream with a separately awaited final result. */
export declare class EventStream<T, R = T> implements AsyncIterable<T> {
  private queue;
  private queueHead;
  private waiting;
  protected done: boolean;
  private resultSettled;
  private finalResultPromise;
  private resolveFinalResult;
  private rejectFinalResult;
  private isComplete;
  private extractResult;
  constructor(isComplete: (event: T) => boolean, extractResult: (event: T) => R);
  push(event: T): void;
  end(result?: R): void;
  [Symbol.asyncIterator](): AsyncIterator<T>;
  result(): Promise<R>;
}
/** Assistant-message event stream that resolves on done/error terminal events. */
export declare class AssistantMessageEventStream extends EventStream<AssistantMessageEvent, AssistantMessage> implements AssistantMessageEventStreamContract {
  private activeThinkingBlocks?;
  push(event: AssistantMessageEvent): void;
  end(result?: AssistantMessage): void;
  private clearThinkingAppends;
  constructor();
}
/** Creates an assistant-message stream for provider and plugin adapters. */
export declare function createAssistantMessageEventStream(): AssistantMessageEventStream;
//#endregion