import { E as NormalizedModelCatalogRow, _ as ModelCatalogProvider, a as ModelCatalog, h as ModelCatalogModel, v as ModelCatalogSource } from "./model-catalog-types-DOa2_UCx.mjs";
//#region packages/model-catalog-core/src/model-catalog-reasoning.d.ts
/** Preserve OpenRouter's declared effort choices and mandatory-thinking wire contract. */
declare function normalizeOpenRouterModelReasoning(value: unknown): Pick<ModelCatalogModel, "reasoning" | "compat" | "thinkingLevelMap"> | undefined;
//#endregion
//#region packages/model-catalog-core/src/model-catalog-normalize.d.ts
/** Normalize a raw model catalog object for the set of providers owned by a plugin/manifest. */
declare function normalizeModelCatalog(value: unknown, params: {
  ownedProviders: ReadonlySet<string>;
}): ModelCatalog | undefined;
/** Normalize one provider catalog into sorted runtime rows. */
declare function normalizeModelCatalogProviderRows(params: {
  provider: string;
  providerCatalog: ModelCatalogProvider;
  source: ModelCatalogSource;
}): NormalizedModelCatalogRow[];
//#endregion
export { normalizeModelCatalogProviderRows as n, normalizeOpenRouterModelReasoning as r, normalizeModelCatalog as t };