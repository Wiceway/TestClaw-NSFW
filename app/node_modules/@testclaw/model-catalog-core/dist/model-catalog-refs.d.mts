import { normalizeProviderId } from "./provider-id.mjs";
//#region packages/model-catalog-core/src/model-catalog-refs.d.ts
export type ModelCatalogRef = {
  provider: string;
  modelId: string;
};
export type ProviderModelRef = {
  provider: string;
  model: string;
};
/** Recognizes one unambiguous hosted source suffix on a bare or qualified model ref. */
export declare function isCloudModelRef(modelRef: string | undefined): boolean;
/** Build a provider/model catalog reference. */
export declare function buildModelCatalogRef(provider: string, modelId: string): string;
/** Parse a strict provider/model reference without normalizing either segment. */
export declare function parseProviderModelRef(value: string): ProviderModelRef | null;
/** Parse a strict provider/model catalog reference. */
export declare function parseModelCatalogRef(value: string): ModelCatalogRef | null;
/** Build a case-insensitive merge key for provider/model rows. */
export declare function buildModelCatalogMergeKey(provider: string, modelId: string): string;
//#endregion
export { normalizeProviderId as normalizeModelCatalogProviderId };