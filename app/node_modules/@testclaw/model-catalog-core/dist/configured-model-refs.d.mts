//#region packages/model-catalog-core/src/configured-model-refs.d.ts
/** One configured model reference plus its config path and field-owned syntax. */
export type ConfiguredModelRef = {
  path: string;
  value: string;
  kind: "selector" | "literal";
};
/** Agent config keys that can contain direct model references. */
export declare const AGENT_MODEL_CONFIG_KEYS: readonly ["model", "utilityModel", "imageModel", "voiceModel", "pdfModel"];
/** Visit raw selector refs without changing values, order, or fallback indices. */
export declare function visitModelSelectorRefs(value: unknown, path: string, visit: (path: string, value: string, role: "primary" | "fallback") => void): void;
/** List raw refs from one string or primary/fallback model selector. */
export declare function listModelRefsFromConfigValue(value: unknown): string[];
/** Collect configured model references from agents, tools, channels, hooks, and message config. */
export declare function collectConfiguredModelRefs(config: unknown, options?: {
  includeChannelModelOverrides?: boolean;
}): ConfiguredModelRef[];
/** Collect only configured model reference values. */
export declare function collectConfiguredModelRefValues(config: unknown, options?: {
  includeChannelModelOverrides?: boolean;
}): string[];
//#endregion